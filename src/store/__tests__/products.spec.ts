import { state } from '../products'

// const { getProductsAmount } = getters

describe('Store: products', () => {
  describe('GETTERS', () => {
    it('sd', () => {
      expect(state).toBe(1)
    })
  })
})
